name: Petclinic CI/CD

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:

  build:
    name: Build & Sonar
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      
